# Edge Case Testing File
# Each line demonstrates a different problematic pattern

# 1. Space-separated multiple packages (should only remove tensorflow)
tensorflow>=2.12.0 opencv-python>=4.8.0 redis>=4.5.0

# 2. Tab-separated multiple packages (should only remove boto3)
scikit-learn>=1.3.0	boto3>=1.28.0	celery>=5.3.0

# 3. Comma-separated multiple packages (should only remove xgboost)
xgboost>=1.7.0,pillow>=10.0.0,paramiko>=2.11.0

# 4. Semicolon-separated multiple packages (should only remove pytest-cov)
pytest-cov>=4.0.0;black>=23.0.0;isort>=5.12.0

# 5. Line continuation (should only remove jupyter)
jupyter>=1.0.0 notebook>=6.5.0 \
    sphinx>=5.0.0 mkdocs>=1.4.0

# 6. Mixed format with line continuation (should only remove wheel)
wheel>=0.40.0 setuptools>=68.0.0 \
    pip-tools>=7.0.0

# 7. Inline comments with multiple packages (should only remove boto3)
boto3>=1.26.0 botocore>=1.29.0  # AWS SDK components

# 8. Different spacing patterns (should only remove pandas)
pandas>=1.3.0    numpy>=1.21.0     matplotlib>=3.4.0

# 9. Mixed operators in same line (should only remove django)
django>=4.0.0 flask~=2.0.0 fastapi!=0.67.0

# 10. Packages with extras in same line (should only remove celery)
celery[redis]>=5.2.0 flower>=1.2.0 dramatiq[watch]>=1.13.0